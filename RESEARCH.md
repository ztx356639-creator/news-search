# news-search · 需求研究（RESEARCH）

> 文档版本：v1.2 · 最后更新：2026-05-27  
> 当前实现：前端 Mock 三卡（`client/`），后端待建

---

## 一、项目目标

开发一个多平台热点聚合网站，聚合：

- 小红书热榜
- B站热搜
- 知乎热榜

未来扩展：

- AI 热点资讯
- 医药行业动态
- QC 行业热点

目标：

让用户通过一个页面，快速获取大众热点 + AI + 专业行业信息。

---

## 二、为什么做这个项目？

当前热点信息分散：

用户需要打开：

小红书 → 知乎 → B站 → 行业资讯

效率低。

希望实现：

一个入口浏览多个平台热点。

同时作为：

1. Vibe Coding 学习项目
2. React + Express 全栈实践
3. AI + 行业资讯作品集

---

## 三、目标用户

### 用户1：普通热点关注者

需求：快速浏览热门信息（榜单、热度、更新时间）。

### 用户2：AI 从业者

需求：关注 AI 新闻、大模型动态、Agent 信息。

### 用户3：医药/QC 行业人员

需求：关注行业动态、医药资讯、自动化趋势、AI 应用。

> **产品注意**：用户 2/3 长期可能需要筛选、订阅、收藏；见第五节「MVP 不做 vs 路线图」，避免与扩展目标冲突。

---

## 四、核心需求（MVP）

### 4.1 前端（已实现 / 待对接 API）

| 项 | 要求 | 状态 |
|----|------|------|
| 首页标题 | 「今日热搜」+ 副标题 | ✓ Mock |
| 三张热点卡 | 小红书 / B站 / 知乎 | ✓ Mock |
| 每平台条数 | Top 10 | ✓ Mock |
| 列表字段 | rank、title、heat | ✓ Mock |
| 卡片底更新时间 | `更新时间：YYYY-MM-DD` | ✓ Mock |
| 排名高亮 | 1~3 名视觉强调 | ✓ |
| 布局 | 桌面 3 列 Grid；手机 1 列 | ✓ |
| 样式 | 圆角阴影、居中、max-width（1440px） | ✓ |
| 标题 Hover | 悬停变色 | ✓ |
| 页脚 | 学习项目 / 公开信息 / 未来扩展说明 | ✓ |
| 点击跳转 | 外链或 `#` 占位 | ✓ `#` |
| 加载态 | 骨架屏或 Loading | ○ 待做 |
| 错误态 | 单卡失败、全局失败提示 | ○ 待做 |
| 空态 | 无数据时的文案 | ○ 待做 |
| 刷新 | 手动刷新按钮（可选自动轮询） | ○ 待做 |

外链约定（对接真实数据后）：

- 使用真实 `url`，`target="_blank"`
- `rel="noopener noreferrer"`
- 链接失效时保留标题、标注「链接不可用」

---

### 4.2 后端

| 项 | 要求 |
|----|------|
| 接口 | `GET /api/hot` |
| 缓存 | 内存或 Redis，TTL **5~10 分钟** |
| 健康检查 | `GET /health`（部署探活） |
| 超时 | 单平台抓取超时 ≤ 8s，不拖死整页 |
| 并发 | 三平台 **并行** 请求 |
| 部分成功 | 某平台失败时，其余平台仍返回；失败项带 `status: error` |
| 限流 | 自建 API 可选：如 60 req/min/IP |
| 环境变量 | 抓取所需 Key、CORS 白名单、缓存 TTL 走 `.env` |

---

### 4.3 API 契约（前后端共用）

**响应示例：**

```json
{
  "updatedAt": "2026-05-26T08:00:00Z",
  "platforms": [
    {
      "id": "xiaohongshu",
      "name": "小红书热榜",
      "status": "ok",
      "updatedAt": "2026-05-26",
      "items": [
        {
          "rank": 1,
          "title": "春日野餐穿搭灵感",
          "heat": "128.6万",
          "url": "https://example.com/...",
          "updatedAt": "2026-05-26"
        }
      ]
    }
  ]
}
```

**`status` 语义：**

| 值 | 含义 |
|----|------|
| `ok` | 数据正常 |
| `stale` | 使用过期缓存（抓取失败后的降级） |
| `error` | 无可用数据 |

**HTTP：**

- 全部失败 → `503`
- 部分成功 → `200` + 混合 `status`
- 缓存命中 → 响应头 `Cache-Control: public, max-age=300`（可按 TTL 调整）

类型定义与 `client/src/types.ts` 对齐，后续可抽到 `shared/types` 供 Express 复用。

---

### 4.4 部署

| 项 | 要求 |
|----|------|
| 公网 | HTTPS 可访问 |
| 推荐拓扑 | 前端 **Vercel**（静态）+ API **Railway / Render**（Express） |
| CORS | 仅允许生产/预览前端域名 |
| 环境 | `VITE_API_BASE_URL` 指向后端 |

> **架构注意**：Vercel 不适合跑带长缓存的 Express 单体；API 与静态站分离部署。

---

## 五、分阶段里程碑

| 阶段 | 目标 | 交付物 |
|------|------|--------|
| **Phase 0** | 前端 Mock 可演示 | ✓ 当前 `client/` |
| **Phase 1** | 后端骨架 + 1 个平台真数据（或稳定 Mock API） | `server/`、`GET /api/hot` |
| **Phase 2** | 三平台 + 缓存 + 错误降级 | 并行抓取、stale 策略 |
| **Phase 3** | 公网部署 + 健康检查 | Vercel + Railway |
| **Phase 4+** | AI / 医药 / QC 模块 | 见第八节扩展原则 |

---

## 六、数据获取策略（待决策）

> MVP 前必须为每个平台选定方案，避免后端无从下手。

| 平台 | 方案 A | 方案 B | 方案 C | MVP 选用 | 备注 |
|------|--------|--------|--------|----------|------|
| 小红书 | 官方/合作 API | 第三方聚合 | 页面解析 | **待定** | 合规风险高 |
| B站 | 公开热搜 API | 第三方 | 页面解析 | **待定** | 关注接口稳定性 |
| 知乎 | 热榜 API/RSS | 第三方 | 页面解析 | **待定** | |
| AI 资讯 | RSS / Newsletter API | 人工 curated | — | Phase 4+ | |
| 医药/QC | 行业 RSS / 监管公开源 | — | — | Phase 4+ | |

**抓取原则：**

- 优先官方/授权渠道；非官方需评估 ToS 与 robots.txt
- 服务端发起请求（避免浏览器 CORS）
- 请求域名白名单，防止 SSRF
- 失败时：返回 `stale` 缓存 > 显示错误卡 > 绝不静默展示假数据（除明确标注的 Mock 环境）

---

## 七、MVP 范围边界

### 7.1 迭代 1 不做（Backlog，非永久放弃）

× 登录 · × 收藏 · × 搜索 · × 评论 · × 用户系统

先保证：**热点聚合 + 缓存 + 部署**。

### 7.2 路线图（迭代 2+，按用户 2/3 需求评估）

- 按分类/标签筛选（AI、医药、QC）
- 关键词搜索
- 订阅/收藏（需用户系统时再开）

---

## 八、扩展架构原则（避免与未来冲突）

```
┌─────────────┐     ┌──────────────┐     ┌─────────────────┐
│   client    │────▶│  Express API │────▶│ fetchers/       │
│  HotCard    │     │  /api/hot    │     │ per-platform    │
│  FeedCard*  │     │  /api/feeds* │     │ normalizer/     │
└─────────────┘     └──────────────┘     └─────────────────┘
```

| 原则 | 说明 |
|------|------|
| **组件分型** | `HotCard`：榜单（rank + heat）；未来 `FeedCard`：资讯流（摘要、来源、作者） |
| **API 分层** | 社交热榜 `GET /api/hot`；行业/AI 可增 `GET /api/feeds?category=ai` |
| **缓存分 TTL** | 热榜 5~10 min；RSS 可 15~30 min，不强制同一 TTL |
| **目录结构** | `client/` + `server/`（或 monorepo `packages/shared`） |
| **布局预留** | 6+ 模块时：分区 Tab（大众热点 / AI / 行业）或折叠区块，避免无限加列 |

---

## 九、风险与约束

### 9.1 技术风险（已有）

1. **平台接口变化** → 抓取失败，需监控与快速切换数据源  
2. **限流 / 403** → 控制频率、退避重试、缓存兜底  
3. **跨域** → 仅后端抓取，前端只调自有 API  
4. **免费部署限制** → 冷启动、内存、出站请求配额  

### 9.2 补充风险（v1.1 新增）

| 风险 | 影响 | 缓解 |
|------|------|------|
| **合规 / ToS** | 账号封禁、法律纠纷 | 优先授权源；页脚声明；学习用途不商用爬取 |
| **机房 IP 被封** | 线上抓不到、本地正常 | 换节点/代理；第三方 API |
| **数据可信度** | 用户信任下降 | 展示 `updatedAt`；stale 标注；禁止静默假数据 |
| **单点失败** | 整页空白 | 并行 + 部分成功 + 单卡错误 UI |
| **Schema 演进** | AI/QC 字段不兼容 | 统一 normalizer，扩展字段用可选属性 |
| **部署模型混用** | Express 缓存与 Serverless 不匹配 | API 独立部署 |
| **无契约测试** | 接口变更静默损坏 | 共享类型 + 快照/集成测试 |

---

## 十、体验与合规（最小清单）

- [ ] 加载骨架屏  
- [ ] 单平台错误卡（文案 + 重试）  
- [ ] 全局「数据更新于 …」  
- [ ] 页脚：学习项目、公开信息、不官方代言  
- [ ] 外链安全属性（见 4.1）  
- [ ] 基础 SEO：`title`、`description`  

---

## 十一、成功标准（21 天，可验收）

### 必须达到

- [ ] 本地前后端可运行  
- [ ] 公网 HTTPS 可访问  
- [ ] `GET /api/hot` 可用且带缓存  
- [ ] 三平台热点展示（**至少 2 个为真实数据**，或文档明确 MVP 仍为 Mock）  
- [ ] 单平台失败时，其余平台仍可展示  

### 建议达到

- [ ] API 缓存命中 P95 &lt; 500ms  
- [ ] `GET /health` 通过  
- [ ] 前端 loading / error 态  
- [ ] `shared` 或文档化 API 类型与前端一致  
- [ ] AI/QC 扩展点：目录或接口预留，不要求上线  

---

## 十二、当前代码对照

| 文档项 | 代码位置 |
|--------|----------|
| Mock 数据 | `client/src/data/mockHotTopics.ts` |
| 类型 | `client/src/types.ts` |
| 热点卡 | `client/src/components/HotCard.tsx` |
| 页脚 | `client/src/components/SiteFooter.tsx` |
| 首页布局 | `client/src/App.tsx`、`App.css` |

---

## 十三、个人思考

本项目不仅是热点聚合工具，也是个人技术成长路径上的**刻意练习**。

### 13.1 希望通过 news-search 学习

| # | 能力 | 在项目中的落点 |
|---|------|----------------|
| 1 | **Vibe Coding 工作流** | RESEARCH → 实现 → 部署的文档驱动开发；用 AI 辅助拆解需求、写代码、改 RESEARCH |
| 2 | **React + TypeScript** | `client/` 组件化、类型安全、Mock → API 对接 |
| 3 | **Express API** | `server/`、`GET /api/hot`、错误处理与分层 |
| 4 | **缓存** | 5~10 分钟 TTL、stale 降级、缓存命中体验 |
| 5 | **部署** | Vercel + Railway、HTTPS、环境变量、CORS |
| 6 | **AI 与行业资讯结合** | Phase 4+ 扩展 AI 热点、医药/QC 资讯模块 |

### 13.2 与分阶段里程碑的对应

```
Phase 0  Mock 前端          → React + TS + Vibe Coding
Phase 1~2  Express + 缓存   → API + 缓存
Phase 3  公网部署            → 部署
Phase 4+  AI / 医药 / QC    → 行业资讯 + 作品集叙事
```

### 13.3 最终愿景

希望将本项目沉淀为一条清晰的作品集叙事线：

**AI + QC + 自动化**

- **AI**：大模型、Agent、行业内的 AI 应用动态  
- **QC**：质检、合规、医药与制造相关资讯聚合  
- **自动化**：抓取、缓存、定时更新、可观测的「信息流」后端  

news-search 从「三平台热榜」出发，逐步演化为：**大众热点入口 + 专业行业情报站**，既能日常使用，也能在求职/合作中展示全栈与领域理解。

> 与文档其他章节的关系：产品目标见**第一节**，用户画像见**第三节**，技术扩展见**第八节**，验收见**第十一节**。

---

## 附录：修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | — | 初版目标、MVP、风险、成功标准 |
| v1.1 | 2026-05-27 | 补充 API 契约、分阶段、数据策略、扩展原则、补充风险、体验清单、与实现对齐 |
| v1.2 | 2026-05-27 | 新增第十三节「个人思考」：学习目标、阶段对应、AI+QC+自动化作品集愿景 |
