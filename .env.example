# Server (copy to server/.env)
PORT=3001
CACHE_TTL=600
CLIENT_ORIGIN=http://localhost:5173

# Client (copy to client/.env)
# Leave empty in dev to use Vite proxy (/api -> http://localhost:3001)
VITE_API_BASE_URL=
